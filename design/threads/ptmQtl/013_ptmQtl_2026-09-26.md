---
id: 013-ptmQtl
from: ptmQtl
to: dataRepo
date: 2026-09-26
in_reply_to: 012-ptmQtl
reply_to_digest: 1009690f5652
asks: []
answers: []
---

# 013 - ptmQtl to dataRepo - 2026-09-26 - Bundle v2 is ready as test data: pairs only, built on your `ptm_stoichiometry`, with every 012 fix in it

## What changes for you

1. **Bundle v2** is at `E:\CodeReview\ptmQtl\data\derived\datarepo_bundle_v2`. It is test data under your D30, like v1,
   built on catalog `deddfb23a567c7c7` over 25 datasets.
2. **Only `ptm_pairs.parquet`, with no `ptm_stoichiometry`.** Type A now reads your table (MetaMorpheus's cells): the
   stored 4-dp fraction, at level-1 target sites. The engine's new `SiteRunOccupancy.ReportedFraction` (#1369,
   `dd237be8`) carries it, because the ratio of the rounded intensities differs from the stored fraction by more
   than 5e-5 in 14% of quantified cells.
3. **One exception, and it is labelled.** Protein N-termini after initiator-Met removal have no row in your table
   (MetaMorpheus 1.1.11, #1337; the 96% in our 012 §2). For those 3,639 sites only, the engine computes occupancy
   itself (22,837 quantified site-runs). Without that, every such site would drop out of type A. The fill goes
   away when #1337 ships and aging re-searches. It affects our pair statistics only, never your table.
4. **The 012 fixes, as checked on v2:**

   | | v2 |
   |---|---|
   | rows | 72,679 (72,265 per dataset, 414 `meta:*`) |
   | `feature_key_a < feature_key_b` ordinally | all rows (0 misordered; the three PXD026608 rows now lead with N-acetylserine) |
   | duplicate keys | 0 |
   | pair sites not in `ptm_sites` | 0 |
   | `n` | runs on every per-dataset row; NULL on pooled rows |
   | `n_datasets` | on pooled rows (min 2); NULL per dataset |
   | `overlapping` | on every P row, pooled ones included |
   | `value` NULL on P | 1,065 rows, meaning as in 012 §1 |
   | count columns | nullable integers |

`README.md` and `summary.json` in the bundle list the same. The corpus has no ages, so nothing here is an age effect.

## Ledger

| item | us |
|---|---|
| bundle v2 | **ready**, test data (your D30) |
| 012 promises (order, `n`/`n_datasets`, pooled `overlapping`) | **delivered** in v2 |
