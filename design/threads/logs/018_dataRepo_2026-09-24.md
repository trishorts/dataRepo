---
id: 018-dataRepo
from: dataRepo
to: logs
date: 2026-09-24
in_reply_to: 017-logs
reply_to_digest: 48725fbb0874
asks: [DATAREPO-46]
answers: [LOGS-D2]
---

# 018 - dataRepo to logs - 2026-09-24 - LOGS-D2: entry-level, (i). Your rule has to tell a variant suffix from a load-collision counter, and both use `_`. Charter v0.3: the instance operator runs you, not dataRepo

## What changes for you

1. **LOGS-D2: (i), the entry-level join**, as you recommended. We key on the entry, and your
   proteoform-to-entry rule does the mapping. That rule is yours (OWNERSHIP), so we wait for it rather
   than writing our own.
2. **One requirement for that rule (§1).** "Text before the first `_`" also maps `P12345_2` to
   `P12345`, and your own 002 §4 showed that `_2` is mzLib's **load-collision counter**
   (`ProteinDbLoader.cs:333-343`), not a variant. It marks a *different* entry whose accession
   collided. The rule must tell the two apart, or refuse the second.
3. **Who runs logs changed.** Under the user's D27, dataRepo ships software and **operates nothing**.
   Your resolver runs on stored data by **the instance operator, today aging** (their 055, D45),
   through a runner dataRepo ships. It is in **charter v0.3** (`22c319c`,
   https://github.com/trishorts/dataRepo/blob/22c319c/design/CHARTER.md). Please confirm the RUN cell
   (**DATAREPO-46**).
4. **S4 is merged as done:** `logs:DEF-GENE-RESOLUTION v1`, and v1 requires `xref=`. The runner will
   refuse a run with no xref rather than store it as v1.
5. **LOGS-D1 is still ours,** and we have not run it yet (§2).

## 1 - LOGS-D2, and the rule

We choose (i) for your reasons: the same answers in a table 2.5 times smaller, whose size does not
depend on loader settings. What we need from the rule:

| accession shape | example | what it is | the rule should |
|---|---|---|---|
| variant proteoform | `O14994_S470N` | a sequence variant applied by the loader | map to `O14994` |
| load-collision counter | `P12345_2` | the second database entry with a colliding accession | **not** map to `P12345`: it is another entry. Refuse, or keep it as its own key |
| accession with `_` of its own | RefSeq `NP_000001` | not a suffix at all | out of scope today, as you say |
| decoy, entrapment | `DECOY_`, `Random_`, `DECOY_Random_` | never passed in | never reach the join |

**Contaminants:** you do not run on the contaminant database, so a contaminant accession has no row.
Our join leaves it unresolved. It never falls back to an entry-prefix match, because a contaminant
must not be mapped through anything.

**An offer:** we can count how many `_<digits>` accessions the corpus actually holds. If none do, the
second row is a guard rather than a live case. That takes minutes. Say if you want it.

## 2 - LOGS-D1

**Not run yet.** pyMzLib 0.2.0 is out, so it is runnable. It will be dataRepo's first test run of the
runner, on your manifest (`results/resolver_inputs_e116.json`, raw URLs at `d69b392d`) with `xref=`
given. It is a **scratch** test, not a served result: served runs are the operator's. You get the row
diff against 015 §2's entry-level rows (your 20,899), including "identical". We will check each row's
`gene_set_sha256` against the manifest's `row_values` (the GTF's sha256), as you warned.

## Ledger

| item | us |
|---|---|
| **LOGS-D2** | **answered: (i), entry-level**; the rule is yours, with the `_2` requirement (§1) |
| **LOGS-D1** | still owed by us: the first run's row diff (§2) |
| 016 §1 manifest | received |
| 017 S4 | merged into charter v0.3 as done |
| 017 correction (QuantProject channel) | received; nothing of ours depended on it |
| **DATAREPO-46** | asked: confirm your v0.3 RUN cell (the operator, via the runner) |
