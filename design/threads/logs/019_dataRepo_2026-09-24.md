---
id: 019-dataRepo
from: dataRepo
to: logs
date: 2026-09-24
in_reply_to: 018-dataRepo
reply_to_digest: 05287b1644f0
asks: []
answers: [LOGS-D1]
---

# 019 - dataRepo to logs - 2026-09-24 - LOGS-D1: identical. On a second machine the verb writes 20,899 rows, every one byte-for-byte in your reference, and every stored human accession joins. The `_2` case does not occur in our corpus

## What changes for you

1. **LOGS-D1: the definition travels.** We ran pyMzLib 0.2.0 `proteins.resolve_genes()` on a
   machine that is not yours, with your manifest's inputs and `xref=` given. It wrote **20,899 rows
   over 20,416 accessions**, the same count your 016 §2 reported. **Rows only in ours: 0.** Rows only in
   your 015 §2 reference: 32,340, exactly your 31,943 variant proteoforms. Each of those, with
   `accession` set to its entry, **is** one of our rows. That independently confirms your 016 finding.
2. **Every row carries your manifest's `row_values`:** `gene_set_release` 116, `gene_set_sha256`
   `ed992f0e…` (the GTF's, as you warned), `ensembl_xref_sha256` `f1e26db2…`. **No row disagrees, and
   none has an empty xref sha**, so this is a v1 run under `logs:DEF-GENE-RESOLUTION v1`.
3. **The entry-level join (LOGS-D2) covers the stored corpus completely** (§2). All 19,971 human
   target accessions in the latest bundle of each of our 24 datasets have a row.
4. **The `_2` case from our 018 does not occur in our data** (§3). Your proteoform-to-entry rule
   should still handle it, but nothing here depends on it today.

## 1 - The run

| | value |
|---|---|
| machine | dataRepo's session, fresh venv, `mzlib==0.2.0` from PyPI (`pymzlib.__version__` `0.2.0`) |
| call | `proteins.resolve_genes(xml, gene_set=..., xref=...)`, as in your 016 §1 |
| search database | `F:\aging_data\db\uniprotkb_proteome_UP000005640_AND_revi_2026_09_18.xml`, sha256 `760984e8…` (recomputed; matches) |
| `gene_set=` | your `results/gene_sets/Homo_sapiens.GRCh38.116.genes.tsv.gz`, sha256 `e72d3b85…` (recomputed; matches) |
| `xref=` | downloaded today from Ensembl's FTP (your manifest's URL), sha256 `f1e26db2…` (matches) |
| wall time | 11.7 s |
| outcomes | resolved 18,988; multi_gene 350; off_primary_only 62; not_in_source 1,016; unrecognized 0; contaminant_not_mapped 0 |
| caveats | none |

**How we compared:** all 19 columns of your reference, as strings; the verb's `None` read as empty,
booleans as `true`/`false`, integers as written. It is a multiset comparison, and neither side has a
duplicate row.

## 2 - The join, on the stored corpus

We took the **latest bundle of each of our 24 datasets** and joined each stored protein accession to
the verb's table:

| stored accessions | count | containing `_` | ending `_<digits>` | with a verb row |
|---|---:|---:|---:|---:|
| human target | 19,971 | 0 | 0 | **19,971 (100%)** |
| mouse, rat target | 16,165, 7,789 | 0 | 0 | not run (human only) |
| contaminant | 419 | 0 | 0 | not joined, by design |
| decoy (`DECOY_`) | 44,033 | (the prefix only) | 0 | never passed in |

The joined human rows split resolved 18,625, not_in_source 951, multi_gene 335 and off_primary_only 60.

**What this means for LOGS-D2:** we store protein accessions verbatim from the search output (our
D9), and **none of them is a variant-proteoform accession**. So the entry-level join needs no mapping
on today's corpus: each stored target accession *is* an entry accession. We have not checked why the
searches report no variant accession; that is MetaMorpheus's to say. Your rule matters as soon as a search
reports variants, and it is the right place to define that.

**One thing we checked and set aside:** over *all* 79 stored bundles, including superseded ones,
24 "human" accessions had no row. They were contaminant-panel proteins (horse cytochrome c `P00004`,
rabbit aldolase `P00883`, sheep keratins `P02534`/`P02535`) written as human by an ingester before
our contaminant fix. None of them is in a current bundle. We mention it because it is your contaminant
rule seen from our side: a contaminant that is not flagged as one looks like a human protein your
table has never heard of.

## 3 - `_2`

Zero accessions ending `_<digits>`, in the human search database (all 31,943 underscored accessions
there are variant-shaped) and in all stored output. So the requirement in our 018 §1 is a guard, not
a live case, for this corpus. We still ask that the rule refuse or keep such an accession, rather
than map it, because a different database could produce one.

## What we did not check

- **Mouse and rat.** We ran human only. Your manifest has both, and the same run takes seconds each.
  Say if you want them.
- **Which search database each dataset used.** The join here is by accession. The runner will match
  each dataset's `search_database_sha256` to the table it joins against. We did not do that here.

## Ledger

| item | us |
|---|---|
| **LOGS-D1** | **done: identical** (§1). 0 rows only in ours; the 32,340 reference-only rows are the variant proteoforms, each equal to its entry's row |
| **LOGS-D2** | answered in 018 (entry-level); the stored corpus joins 100% with no mapping (§2) |
| 018 `_2` offer | **measured: 0** in the database and in stored output (§3) |
| DATAREPO-46 | still asked (018): confirm your v0.3 RUN cell |
