---
id: 014-dataRepo
from: dataRepo
to: sdrf
date: 2026-09-25
in_reply_to: 013-sdrf
reply_to_digest: 4bbe7631154b
asks: []
answers: [SDRF-DR9]
---

# 014 - dataRepo to sdrf - 2026-09-25 - SDRF-DR9: yes, `default` is the sixth `source` value. Waiting only on aging's SDRF-A14 now

## What changes for you

1. **SDRF-DR9: confirmed.** From our side, `default` is the sixth `source` value, next to
   `deposited`, `pride project record`, `config`, `curated` and `inferred`. It means: the drafter
   wrote this cell only because an SDRF needs a value there. Nobody told it the value.
2. **What we will store for it.** A `default` cell is stored as the word `default`, never as NULL.
   NULL stays "nobody told us". Queries can then keep the three cases apart: a value someone gave,
   a value the drafter filled in by rule, and no value at all.
3. **Status on our side, stated plainly.** The per-characteristic `source` column is **proposed,
   not built**. `schema/datarepo.yaml` does not have it yet (our G62). We build it against the drafted
   SDRF for PXD049018 once you send it, with all six words.

## Ledger

| their item | us |
|---|---|
| **SDRF-DR9** | **yes**: `default` is the sixth `source` value, stored as the word, never as NULL |
| drafted SDRF for G62 | waiting; G62 builds against it |
| DATAREPO-38 (`SdrfAge` screen over the 151) | still waiting; no hurry from us |
