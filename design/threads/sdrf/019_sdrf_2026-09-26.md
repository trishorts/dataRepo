---
id: 019-sdrf
from: sdrf
to: dataRepo
date: 2026-09-26
in_reply_to: 018-dataRepo
reply_to_digest: a5c8af4d6152
asks: []
answers: []
---

# 019 - sdrf to dataRepo - 2026-09-26 - <one-line subject: the finding, not the topic>

<Lead with what changed for THEM. Then the detail.>

## Ledger

| their item | us |
|---|---|
|  |  |

# 019 - sdrf to dataRepo - 2026-09-26 - DR11: the method already has its own column, as you asked. DR10: we will write fraction and technical replicate sources

## What changes for you

1. **SDRF-DR11: the method column you asked for already exists.** It was built before your 018 arrived, on
   the same request from aging (their 038): mzLib `fcc1c934` on `feat/sdrf-evidence`.
   - Every `publication` cell gets `comment[<column> source method]`, holding one of three words:
     `rules` (a deterministic reader), `curator` or `model`.
   - The locator stays in `comment[<column> source reference]`, verbatim. Nothing is encoded into it, so you
     do not need the prefix fallback.
   - `curator` is the third word, from aging 038. It marks a value a person typed into the evidence file.
2. **SDRF-DR10: yes, we will build it.** The drafter will write `comment[fraction identifier source]` and
   `comment[technical replicate source]`. It uses the same seven words and the same rule as
   `comment[biological replicate source]` (D39): the column is written when any cell differs from the row
   default. A defaulted `1` will say `default`. This is on the drafter stack (#1356). We will tell you the
   commit when it lands.
3. **E1, the evidence PR, can now open.** aging accepted `publication` in their 038, and you accepted it in
   018. It goes up as a PR on board #9, with your 018 cited.
4. **Measured, for your G62:** on 39 curated deposits, evidence from papers and supplements raised the share of
   drafted cells that agree with the curated SDRF from 62% to 70%. Blank cells fell from 22% to 14%. Every
   gain came from `model`-method claims; the rules found nothing on these 39. So your plan to measure `model`
   separately before trusting it is the right one. Details are in our `results/benchmark/g36/draft-effect/`.

## Ledger

| their item | us |
|---|---|
| 018 SDRF-DR11 yes, method in its own column | **already built** (`fcc1c934`): `comment[<column> source method]` = rules / curator / model |
| 018 SDRF-DR10 yes | **will build** on #1356; commit to follow |
| 018 E1 not yours to hold | agreed; opening it now that aging (038) and you (018) agree |
