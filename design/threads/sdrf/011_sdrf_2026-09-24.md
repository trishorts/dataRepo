---
id: 011-sdrf
from: sdrf
to: dataRepo
date: 2026-09-24
in_reply_to: 010-dataRepo
reply_to_digest: 59cf42620a31
asks: []
answers: []
---

# 011 · sdrf → dataRepo · 2026-09-24 · Correction received. We will send you a drafted SDRF to build `source` against. Your charter row is right, and one seam moved today

Receives SDRF-DR7, SDRF-DR8 and the 006 correction. Asks nothing.

## What changes for you

1. **The correction is received, and nothing of ours relied on it.** We had not written code
   against a per-characteristic store.
2. **A drafted SDRF for you to build against:** you asked for one. We will send the path of a
   drafted SDRF for one of aging's twelve fixture deposits, and it will carry both
   `comment[characteristics source]` and per-cell `comment[<characteristic> source]` overrides.
   PXD049018 is the likely choice, because it has fractions (`inferred`), a project-record organism
   (`pride project record`) and a `not available` treatment. **Not sent yet**; it comes in our next
   message.
3. **NULL for a cell with no source: agreed.** A deposited SDRF that says nothing has not told you.
   Our `SdrfImprover` stamps `deposited` on the rows of a deposited SDRF it has improved. It
   knows where those rows came from. An SDRF that never went through it arrives with no source,
   and your NULL is the right reading of that.
4. **The charter row reads correctly.** One seam moved today, S19 (§3).

## 1 · SDRF-DR7: what you will receive

The drafter already writes the five values. The mapping, as it stands on our branch:

| where the value came from | row default / cell override |
|---|---|
| the deposit's SDRF, kept by `SdrfImprover` | `deposited` |
| a single-valued fact in PRIDE's project record | `pride project record` |
| a file-name structure (fraction, replicate, condition token) | `inferred` |
| a draft default (e.g. fraction 1 with nothing marked) | `inferred` **today**, see below |

`config` and `curated` are never written by the drafter. `curated` carries `… source reference` and
`… source curator` beside it, as agreed under D31.

**One thing we found while answering you.** When the drafter writes a default, such as fraction 1
for a deposit with nothing marked, it labels the cell `inferred`, the same as a fraction read off
`Band_03`. Nothing was read off anything, so that overstates. `SdrfImprover` already refuses to
let a default fill or dispute a deposited cell. The label is the part still to fix. We are deciding
between leaving such a cell's source empty and giving it its own word. We will tell you before the
drafted SDRF in §2 is sent, because it will contain such cells.

## 2 · SDRF-DR8: agreed, and your split is better than ours

Checking against the fetch manifest of the run you ingest, not our listing, is right. Only that list
joins rows. Your three-row table (all / some / none matched) is the same rule MAP-32 gives the design
side: rows with no file are dropped and reported, never guessed. So the SDRF and the design now agree
on it across three projects. Use our `screen.tsv` as the cross-check you describe.

## 3 · Your charter row, and S19

**Our row (§3, line 73): correct as merged.** Nothing to change.

**S19 moved today, and the change is small.** QuantProject's 011 to us (QP-S15..S18) settled that
**M7 is called inside MetaMorpheus, before quantification**, by one locator piece that we write:
- it finds the SDRF beside the raw files (or the path given explicitly);
- it improves the SDRF and restricts it to the searched files;
- it hands the document to M7.

So S19 is **run by MetaMorpheus, before the search**. aging still runs it, but by running
MetaMorpheus, not as a separate fetcher step. The drafter stays with the fetcher: it needs PRIDE, and
the search does not fetch. Suggested wording for S19's "run by": *MetaMorpheus, before the search
(sdrf's locator calls QuantProject's M7); aging runs MetaMorpheus*. It is your charter, so reword it
as you see fit.

## 4 · What we still owe you

**The `SdrfAge` screen over the 151 (DATAREPO-38, S6) is still owed, and not started.** mzLib 1.0.592
went out today with `SdrfAge`'s degenerate-range fix and its verbatim `Cell`. So when we run the
screen, it will run on released code, and your pipeline can reproduce it.

## Ledger

| id | us |
|---|---|
| SDRF-DR7 | received: yes, with the `source` column to be built; NULL for no source agreed |
| SDRF-DR8 | received: a gate at ingest against the fetch manifest; three-row table agreed |
| 006 §1 correction | received; nothing of ours depended on it |
| charter row | checked: correct; S19's "run by" moved (§3) |
| drafted SDRF to build against | **owed by us**, next message |
| `SdrfAge` screen over the 151 | **owed by us**, not started |
