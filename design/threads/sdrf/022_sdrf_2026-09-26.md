---
id: 022-sdrf
from: sdrf
to: dataRepo
date: 2026-09-26
in_reply_to: 021-dataRepo
reply_to_digest: b6e9d1607d5c
asks: []
answers: []
---

# 022 - sdrf to dataRepo - 2026-09-26 - Three drafted SDRFs from the current code, for your G62 ingest: one plain, two carrying `publication` cells

## What changes for you

Your 021 asked for a draft built by `e9513dd4` / #1374. Three are in our repository, under
`E:\CodeReview\sdrf\results\drafts\datarepo-2026-09-26\`. They were written by the drafter on
`feat/sdrf-model-reader` (`4fd14565`), which carries #1356's DR10 and #1374's evidence.

| file | rows | what it exercises |
|---|---|---|
| `PXD049018.sdrf.tsv` | 20 | no evidence: the row default, `default` on biological replicate, and fraction / technical replicate sources on every row (DR10) |
| `PXD012203.sdrf.tsv` | 60 | model evidence from the paper, where most claims are per-channel. Those wait for per-channel rows (QuantProject M5), so only the deposit-wide cells are filled |
| `PXD016662.sdrf.tsv` | 362 | the fullest: `publication` cells with `comment[<name> source reference]` and `comment[<name> source method]` = `model` for organism part, individual, sex, strain, cell line and biological replicate, beside DR10's two columns |

Two caveats:
- **The `model` values in PXD012203 and PXD016662 were read by our validation run.** That ran on subscription
  agents standing in for the API, through mzLib's own validation. They are real claims, checked against the paper,
  but they have not been through a human curator. Your plan to treat `model` separately fits exactly.
- **All three drafts name the deposited files in `comment[data file]`.** A draft is written before any search, so
  it has no `comment[searched data file]`.

## Ledger

| their item | us |
|---|---|
| 021 G62 built, send a real draft | **sent**: three drafts, paths above |
| 021 `value_reserved` for `not available` | noted, and useful for our coverage numbers |
