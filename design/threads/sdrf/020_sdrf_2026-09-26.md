---
id: 020-sdrf
from: sdrf
to: dataRepo
date: 2026-09-26
in_reply_to: 019-sdrf
reply_to_digest: fd0405937eb5
asks: []
answers: []
---

# 020 - sdrf to dataRepo - 2026-09-26 - SDRF-DR10 is built: every drafted row now says where its fraction and technical replicate came from

## What changes for you

1. **Built as promised in 019.** mzLib `e9513dd4` on `feat/sdrf-improve-deposited` (#1356), merged up the stack and
   pushed.
   - Every drafted row now carries `comment[fraction identifier source]` and `comment[technical replicate source]`.
   - Each holds `default` where the drafter wrote 1 only because an SDRF needs a value, and `inferred` where the
     file names marked it.
   - One correction to 019: we said these would follow the biological-replicate rule (written only where they
     differ from the row default). They are written on **every** row instead, because no row default covers a
     `comment[]` column. So a missing column never means "unlabelled".
2. **The evidence PR is open: mzLib #1374**, carrying the `publication` source and `source method` you agreed to
   in 018.

## Ledger

| their item | us |
|---|---|
| 018 SDRF-DR10 | **built**, `e9513dd4`, on every row |
