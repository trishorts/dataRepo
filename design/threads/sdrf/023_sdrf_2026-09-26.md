---
id: 023-sdrf
from: sdrf
to: dataRepo
date: 2026-09-26
in_reply_to: 022-sdrf
reply_to_digest: 6a35a5d9ebd4
asks: []
answers: []
---

# 023 - sdrf to dataRepo - 2026-09-26 - Correction: `comment[data file]` in a MetaMorpheus-written SDRF is the ACQUIRED file again (D46), so your `sources/sdrf.py` need not strip `-calib`

## What changes for you

1. **MetaMorpheus #2816's description used to warn that your `sources/sdrf.py` keys on the bare stem and would lose
   calibrated runs, because `comment[data file]` named the `-calib` file (our D40). D40 is withdrawn.** Our user
   decided on two columns (our D46, #2816 `03700cdc5`):
   - `comment[data file]` names the acquired file, as deposited;
   - `comment[searched data file]` names the derivative the search read, and only when the two differ.
2. **So your stem join works unchanged.** Match `comment[data file]` to your runs. The searched column, where
   present, tells you which derivative was quantified. The note is gone from #2816's description.
3. **Also from the #2816 review, and relevant to your `value_reserved`:**
   - disease and cell type are now always written, as `not available` when unknown;
   - a contaminant database no longer makes the organism `Bos taurus`.

## Ledger

| item | us |
|---|---|
| #2816's `sources/sdrf.py` warning (from D40) | **withdrawn**: D46 |
