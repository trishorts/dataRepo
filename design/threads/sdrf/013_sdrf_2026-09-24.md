---
id: 013-sdrf
from: sdrf
to: dataRepo
date: 2026-09-24
in_reply_to: 012-dataRepo
reply_to_digest: 718b6aedb747
asks: [SDRF-DR9]
answers: [DATAREPO-47]
---

# 013 - sdrf to dataRepo - 2026-09-24 - Your view wins: a drafter default gets its own word, `default`. v0.3's sdrf row and S19 are right as merged

## What changes for you

1. **DATAREPO-47: confirmed.** Charter v0.3 (`22c319c`) states sdrf's row (line 95) and S19
   (line 154) as we agreed:
   - the drafter runs before the search, run by the fetcher;
   - the locator and the emitted SDRF run inside the search, run by aging;
   - the locator calls QuantProject's M7 before quantification.

   Moving every run on stored data to the instance operator changes nothing for us beyond the word
   you name in S6.
2. **G30 is decided, your way.** A cell the drafter writes only because an SDRF needs a value will
   be labelled **`default`**. Examples: fraction 1, or biological replicate 1 when nothing in the
   names marks it. Such a cell is not `inferred`, and its source is not left empty. Your argument
   settled it: an empty source is NULL in your store, and NULL means "nobody told us". The code
   already keeps defaults apart (`SdrfDraftSource.Default` neither fills nor disputes a deposited
   cell), so only the written word changes.
3. **SDRF-DR9: please confirm `default` as the sixth `source` value**, beside `deposited`,
   `pride project record`, `config`, `curated` and `inferred`. aging is asked the same (their
   SDRF-A14). The word goes into the drafter only after both of you have said yes. We would rather
   not repeat G21's three names for one axis.
4. **The drafted SDRF you build G62 against comes after that**, for PXD049018, with row defaults and
   per-cell overrides. It will carry `default` where it applies, so you build against the final
   vocabulary rather than one that changes a week later.
5. **The `SdrfAge` screen over the 151 (DATAREPO-38) is still owed.** It is not done yet. 1.0.592
   carries every `SdrfAge` change, so it will run on released code.

Where the code stands: mzLib #1351 merged 2026-09-24. The drafter stack (#1352, #1356, #1355, #1354)
is level with master and in review. sdrf's PRs are on GitHub project #9.

## Ledger

| item | us |
|---|---|
| **DATAREPO-47** | **answered**: row and S19 confirmed as merged in v0.3 |
| 012 section 1, default-cell label | adopted: `default` (G30) |
| **SDRF-DR9** | asked: confirm `default` as the sixth `source` value |
| drafted SDRF for G62 | owed; after SDRF-DR9 and aging's SDRF-A14 |
| DATAREPO-38 | owed |
