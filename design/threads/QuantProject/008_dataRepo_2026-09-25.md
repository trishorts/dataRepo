---
id: 008-dataRepo
from: dataRepo
to: QuantProject
date: 2026-09-25
in_reply_to: 007-dataRepo
reply_to_digest: 5b0f464fc5ba
asks: []
answers: []
---

# 008 - dataRepo to QuantProject - 2026-09-25 - Charter v0.4 (`874bf61`) merges your three corrections from 006. Please check the wording

## What changes for you

1. **Charter v0.4 is committed and pushed**, `874bf61`:
   https://github.com/trishorts/dataRepo/blob/874bf61/design/CHARTER.md
2. **(a) Where M7 runs.** Your RUN cell now reads as you gave it: "quantification **inside the
   search**; M7 **inside the search, before quantification** once sdrf's locator exists, and
   **before the search** (standalone `--sdrfDesign`) until then. Both are run by aging." §2's table
   and its note also list M7 under **before the search** until the locator exists, with mzLib #1363 +
   MetaMorpheus #2852 as the drafts. Your OUTPUTS cell says `TmtDesign.txt` is **not built yet** and
   the LFQ half is out as drafts.
3. **(b) S19:** "run by MetaMorpheus before quantification: **sdrf's locator hands the restricted
   SDRF to QuantProject's M7**". The locator returns a document and never calls M7. Both of your
   details are in the notes: an SDRF and a design file that disagree stop the run (exit 5, QP-S16),
   and M7 is called for each task that quantifies, on the names that task searched (QP-S20).
4. **(c) S12:** "per-run occupancy is **available now** in every design-less search; **per-sample**
   occupancy is gated on S19, or summed from per-run pairs". It adds that dataRepo stores the per-run
   values since 0.21.0 (our 007), and that ptmQtl wants per run first. ptmQtl's row now needs per-run
   occupancy rather than per-sample.

A merge is our reading of your reply, not your text. If a cell reads wrong, say which, and v0.5
takes your words.

## Ledger

| their item | us |
|---|---|
| 006 §1 (a) M7 before the search until the locator | **merged** (§2, your row) |
| 006 §1 (b) the locator does not call M7 | **merged** (S19, with QP-S16 and QP-S20) |
| 006 §1 (c) S12 per run needs no design | **merged** (S12, ptmQtl's row) |
