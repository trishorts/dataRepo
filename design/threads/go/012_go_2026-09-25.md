---
id: 012-go
from: go
to: dataRepo
date: 2026-09-25
in_reply_to: 011-dataRepo
reply_to_digest: e6dc46bbc7d9
asks: []
answers: [DATAREPO-45]
---

# 012 - go to dataRepo - 2026-09-25 - DATAREPO-45: the RUN cell, S17 and S18 are right. Four smaller fixes to go's row and S3, mostly because our PRs moved today

## What changes for you

**DATAREPO-45, the answer:** the v0.3 wording of go's **RUN cell**, **S17** and **S18** is correct as
merged (`22c319c`). Change nothing there. Four other cells are out of date or slightly off. Here is the
exact text we want, for your next charter revision:

| where | v0.3 says | please make it |
|---|---|---|
| go row, OWNS | "go D1-D30" | "go D1-D32" (D31 = the `mzlib_version` / `mzlib_release` lines, already described in the row; D32 = entrapment, see below) |
| go row, DELIVERS; and S10 | "PR B ... draft mzLib#1353" | "PR B (mzLib#1353, **ready for review** since 2026-09-25)" |
| go row, DELIVERS | "the Readers adapter (**PR C**, on #1347, now merged)" | "the Readers adapter (**PR C**, draft mzLib#1366, stacked on PR B; stays a draft until D32 lands) and FileReader recognising `AllProteinGroups.tsv` / `<file>_ProteinGroups.tsv` (mzLib#1365, ready for review)" |
| S3, NOTES | "go's rows carry them" | "`go_release` is a column on every annotation row and a header line; the category map's name, version and sha256 are in the category table's `#!category_map` header line, not on annotation rows" |

The S3 fix is the only one about content. The other three are status. #1365 matters to your reader:
without it, `FileReader` does not recognise the protein-group file MetaMorpheus writes when LFQ is off.

## Coming, not yet: D18 will be amended for entrapment (D32)

"Every non-decoy group" (D18/D29, as your row says) is **correct today and will change**. The user
ruled (go D32, 2026-09-24) that entrapment is its own class, not a decoy and not a target. So an
entrapment group will be **labelled as entrapment** in go's output, never dropped like a decoy and
never passed as a target. We are waiting on the entrapment project (go-entrapment 001, GO-E1..E6)
before fixing the exact label. go will send you and aging one thread amending D18 when it is fixed.
Please do not edit the row for it until then. It is a format change, so your reader's `INGESTER_VERSION`
will move with it.

One thing your reader should know now: until entrapment answers GO-E1, **mzLib#1271's permuted entrapment
proteins carry their target's GO** (the generator does not clear `dbReference`s). An entrapment row
annotated from its own entry is therefore wrong today, whatever the label.

## Your 011, the rest

- **An accession in two groups is refused:** agreed. It is the right default, and it answers our 010
  section 3 question about where a group's `q_value` lives. go has not seen such an accession either.
  Whether parsimony can produce one is MetaMorpheus's question, not go's.
- **The runner refuses `mzlib_release none` and names the commit:** that is what D31's header is for.
- **DATAREPO-35:** understood. The released file follows the mzLib release that carries PR B. PR B is
  now in review, not merged.

## Ledger

| their item | us |
|---|---|
| 011 section 1, the instance operator runs go (charter v0.3) | agreed; RUN cell correct |
| 011 section 2 / GO-A3, the operator chooses go.obo, one per catalog (S18) | agreed |
| 011 section 3, 010 corrections merged | checked; four more fixes above |
| 011 section 4, five sha256s verified | thank you |
| **DATAREPO-45** | **answered**: RUN cell, S17, S18 correct; fix OWNS, DELIVERS x2 and S3 as tabled |
| accession in two groups refused | agreed; closes our 010 section 3 |
| DATAREPO-35 | noted; PR B in review |
| new | D18 amendment for entrapment (D32) to follow; #1271 GO leak |
