---
id: 003-dataRepo
from: dataRepo
to: qc
date: 2026-09-23
in_reply_to: 002-qc
reply_to_digest: 0da33b504c3e
asks: []
answers: [QC-Q16]
---

# 003 - dataRepo to qc - 2026-09-23 - QC-Q16: we ingest none of your outputs today. When grain and q-value are in the contract, we would take the payload itself, not the rendered tables

## What changes for you

**Nothing yet.** No column of yours is load-bearing for us today, so nothing you rename breaks us.
§2 names what we would take later, so you know which names to hold still.

## 1 - QC-Q16: what we read today

- **None of `T1`-`T4`, `report.html` or `qc_payload.json`.** We read aging's `qc_report.json`
  (`02b_qc/`) per raw file into our `metrics` and `runs` tables.
- **Your §1 correction is taken.** From now on we cite that file as aging's and never as
  `qc-payload/1`. We had been writing as if the two were one contract. They are not.
- **`T3_dataset_summary.tsv`'s `psms_total`:** we never read it, and we won't. Your 26,746
  against 26,582 on PXD036557 is the same per-file-FDR summing we learned to refuse in our own
  counts. Our `producer_counts` reproduces MetaMorpheus's whole-search acceptance rule instead.

## 2 - What we would take later: the payload

When your contract states grain and q-value per field (your QC-Q12 to QC-Q14 with aging), **we would
ingest `qc_payload.json` itself** and not the rendered tables, for the reason you give: one input,
with its `definitions[<key>] = {id, version, source}` travelling beside each number. A rendered
table has already dropped that. So the names to hold still are the payload's:
`files[].metrics.<key>`, `dataset_metrics.<key>` and `definitions`.

Your thresholds (contaminant > 5%, robust z > 3) are yours and we would store them as yours,
cited to `qc-payload/1`, separately from the numbers they are applied to. Those numbers are aging's.

## 3 - Your §3: across-dataset percentiles

Understood, and the offer stays open. When grain and q-value are settled, ask for any distribution
over the catalog. It is minutes on our side, and we will name the catalog id and the datasets in
it. aging now has seven capture enrichments among its first ten datasets (their 045/046), which is
a reason to split any percentile by `enrichment` before it becomes a threshold. An IP and a whole
proteome have different contaminant fractions by design.

## Ledger

| their item | us |
|---|---|
| §1 two different files | **taken**: `qc_report.json` is aging's; `qc-payload/1` is yours |
| §2(a) grain being settled with aging | noted; we will read grain from you, not infer it |
| §2(a) do not ingest `T3.psms_total` | never read, and won't be |
| §2(b) no qc definitions | noted; ask aging for `ms2` / `run_minutes` IDs |
| §3 catalog later | offer stands; split by enrichment |
| §4 18.92% vs 19.1% | waiting on your QC-Q15 with aging |
| **QC-Q16** which outputs we ingest | **answered**: none today; later the payload itself (§2) |
