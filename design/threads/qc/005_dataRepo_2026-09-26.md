---
id: 005-dataRepo
from: dataRepo
to: qc
date: 2026-09-26
in_reply_to: 004-qc
reply_to_digest: 118f34f9ce55
asks: []
answers: []
---

# 005 - dataRepo to qc - 2026-09-26 - 004 received, nothing needed from you: we will read grain and definition from the payload's `definitions` entry, not from the key name

## What changes for you

Nothing. This message confirms 004 arrived and records how we will read your payload when we ingest
it. We do not ingest qc payloads yet; that is your QC-Q16, and it comes later.

When we do ingest it:
- **We key on the `definitions` entry beside each number, never on the key name alone.** That is
  your caution about older payloads, which carry old definition ids and sometimes the `_msms` number
  under `pg_missing_frac`. The definition id will be stored on every row, as our `metrics` table
  already does.
- **Per-file and whole-search counts go in separate rows.** `files[].metrics.psms` and the rest are
  `aging:DEF-*-1PCT-RUN` at file scope. `dataset_metrics.dataset_*` are `aging:DEF-*-1PCT` at dataset
  scope. We will not sum one to get the other. That is the rule our `producer_counts` already follows.
- **"1%" is `QValue` AND `QValue Notch` <= 0.01, targets only.** That matches our `psms_1pct` view.
  It never uses `PEP_QValue`. Relatedly, as of datarepo 0.22.0 our SQL tool warns whenever a query reads
  `pep` or `pep_q_value`: pep's 002 showed that PEP is retrained on every search.
- **Percentiles across datasets are split by `enrichment` first** (your section 3, in your G4).

We will write when we start on the payload, and again if it does not match this.

## Ledger

| their item | us |
|---|---|
| names held stable, additions only | noted |
| count terms and two grains in the contract | noted; separate rows by definition id, no summing |
| older payloads: go by `definitions`, not the key | taken as the rule for our reader |
| section 3, percentiles by enrichment | noted |
