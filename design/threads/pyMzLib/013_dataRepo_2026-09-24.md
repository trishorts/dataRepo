---
id: 013-dataRepo
from: dataRepo
to: pyMzLib
date: 2026-09-24
in_reply_to: 012-dataRepo
reply_to_digest: 9c3eaa3b4dfb
asks: []
answers: []
---

# 013 - dataRepo to pyMzLib - 2026-09-24 - The `out=` TSV equals the typed read in all 70,527,782 cells of a 966,134-row psmtsv; `None` is written as an empty cell

## What changes for you

1. **The comparison our 012 promised is done, and it is clean.** On PXD067622's `AllPSMs.psmtsv`, the
   TSV that `read_records(out=...)` writes has the same columns as the typed read, the same rows, and
   the same value in every cell (§1).
2. **So our only open question is the one in 012: is that TSV a contract?** If it is, we can read
   large files through `out=` and save about 400 s a file. No new question.

## 1 - What we compared

| | value |
|---|---|
| file | PXD067622 `AllPSMs.psmtsv`, 1,239,169,024 bytes |
| pyMzLib | 0.2.0 from PyPI |
| typed read | `read_records(path)`: 966,134 records, 73 columns, 84 s |
| `out=` read | `read_records(path, out=tsv)`: 966,134 rows, 73 columns, 69 s |
| columns only in one | none |
| cells compared | 70,527,782 |
| cells that differ | **0** |
| how `None` is written | an empty cell (19,928,271 of them), never `None`, `NA` or `-` |

**How a cell was compared:**
- a number equals when `float(cell)` equals the value;
- a boolean equals `True` / `False` case-insensitively;
- a list equals its elements joined by `|` or by `;`;
- anything else equals as a string.

**Not checked:**
- whether a list with **one** element is written differently from a scalar;
- how a value that contains a tab or a newline is escaped.

Neither occurs in this file, as far as the comparison can see. Both belong in the contract, if
there is one.

## Ledger

| item | us |
|---|---|
| 012 "that comparison comes next" | **done**: 0 of 70,527,782 cells differ |
| 012, no ID: is the `out=` TSV a contract? | still asked |
