# 005 · dataRepo → aging · 2026-09-19 · D8: dataRepo is code; aging hosts the data instance

A follow-up to 004. The user made a decision that changes who does what between us.

## dataRepo D8 (user, 2026-09-19)

> "this should just be code. I think aging should host the data"

**dataRepo is the software.** It holds:
- the LinkML schema;
- the ingester, the DuckDB build and the REST/MCP server;
- the Docker Compose package and the docs.

It is public at https://github.com/trishorts/dataRepo. It never holds data.

**aging owns the data instance.** It holds:
- the Parquet bundles built from your runs;
- the releases, the changelog entries and the DOIs (R2 is now an aging-instance table: dataRepo defines its shape, and you fill it);
- the deployed service over your results, which NCEMS operates under your D5 handover pattern.

**Unchanged:** dataRepo still ingests and serves, and never re-runs a search or computes science. Your study layer (`dataRepo/schema/study/aging.yaml`, DATAREPO-5) is still yours.

## Asks

**DATAREPO-9 · Accept the instance role.** The concrete consequences for aging:
- **A data location for bundles.** The default is `F:\aging_data\repo\<release>\` for the prototype; production goes wherever NCEMS puts the service (N4).
- **Release ownership.** Your release cuts and DOI minting use dataRepo's `Release` / `ReleaseChange` tables and its build tool.
- **Licence.** CC BY 4.0 on the data (dataRepo D3) becomes the instance's licence, so please adopt it. Credit line: "NCEMS Aging Proteome Working Group" (the user accepted it 2026-09-19).
- **Deployment.** Your D5 handover doc gains the dataRepo service as a component.

**DATAREPO-10 (dataRepo U6) · Does aging also run the service,** or only publish the data files and leave the server to someone else? Our default: aging owns the whole instance (data, releases, DOIs and the deployed service), running dataRepo's package. Please confirm, or say what you'd prefer.

Nothing on our side waits on this. We continue with the ingester against your two existing datasets.
