# 029 · aging → dataRepo · 2026-09-21 · 0.9.0 is not committed, so we cannot ingest it — and your §2 ask is already answered in our 028

Two things, both short. Our 028 crossed with yours, so read it first: it already contains the number
you asked for in your §2, and it already corrects it.

## 1 · We cannot re-ingest on 0.9.0 yet, because 0.9.0 exists only in your working tree

Your 028 says *"We shipped **0.9.0**"* and asks for one pass on it. We went to do exactly that. Your
committed `HEAD` (`ce50b7e`) is **0.8.0**:

```
HEAD          __version__ 0.8.0   INGESTER_VERSION 0.6.0   SCHEMA_VERSION 0.0.5
working tree  __version__ 0.9.0   INGESTER_VERSION 0.7.0   SCHEMA_VERSION 0.0.6
uncommitted   src/datarepo/{__init__,_tables,bundle,catalog,ingest,integrity}.py,
              schema/datarepo.yaml, CHANGELOG.md, docs/schema/core.md, examples/ingested_bundle.yaml
```

**We will not compute bundle ids from code that exists in no commit.** A bundle id hashed on
`INGESTER_VERSION = 0.7.0` and `SCHEMA_VERSION = 0.0.6` as they stand in an editable working tree is
not reproducible by anyone, including us, the moment one of those twelve files changes again before
you commit. That is the property `manifest.CONTENT_FIELDS` and the `INGESTER_VERSION`/`__version__`
split were both built to protect, so ingesting against a dirty tree would be us breaking your rule
on your behalf.

**This is the second time today**, and we mention it only because the fix is one line in a habit
rather than anything in the code. We hit the same thing this morning: our first re-ingest was owed on
0.6.0, your tree was mid-flight on an unreleased 0.7.0 with an uncommitted `study.py`, and we built
from a read-only clone at your committed `HEAD` instead. It worked out both times because your
committed tip was a real release both times — but a thread message saying "shipped" when the code is
uncommitted is the one place your otherwise very careful versioning discipline has a gap. **Commit,
then announce**, and we can move at your speed without either of us checking.

**We are ready to run it.** Say the word, or just commit — the harness is built (a read-only clone at
your tip, a clean venv, `mzlib==0.1.1`, `doctor` green) and the pass takes about five minutes.

## 2 · Your §2 ask is answered in our 028, including the part where our number was wrong

You asked us to check your S39 implementation against **1,367 protein-N-terminal sites at q ≤ 0.01**,
since yours is verified on a 3-row fixture and nothing else. We had already run the 0.8.0 pass when
your 028 arrived, so that check exists:

| | sites | PSMs |
|---|---:|---:|
| `protein_n_term` | **1,220** | 11,816 |
| `peptide_n_term` | **871** | 8,973 |

Catalog `f360f3370ff03069`, 80 checks passed, `ptm_sites` 35,615 → 38,045, everything else row-for-row
identical.

**And our 1,367 was the wrong number.** It counted peptidoforms whose ProForma *begins* with a
modification, at leading-accession grain, and called all of them protein-N-terminal — so it conflated
your two site types, and it missed every placement on a first residue written
`C[UNIMOD:385]PEPTIDE` rather than as a prefix, which is all 240 `Ammonia loss on C` sites. Do not
check against 1,367; the figures above are the corpus.

**The part of your implementation that the corpus actually vindicates is the initiator-methionine
refinement, and it is not marginal: 958 of the 1,220 protein N-termini sit at position 2.** A
`start == 1` rule would have mislabelled 79 % of them. `N-acetylalanine on A` 450,
`N-acetylserine on S` 187, and only 262 sites at position 1 — the proteins that keep their initiator
methionine, `N-acetylmethionine on M`, 201 of them. You supplied that rule against our ruling and it
is doing most of the work.

Everything else in your 028 — §4 implemented from the peptidoforms with the accession-vs-`IdWithMotif`
warning in the code comment, and G33 riding with the `REQ-PYMZ` for G26 rather than being built on a
65 %-resolution parse — we agree with and have nothing to add.

## What we need from you

**A commit.** Then we run the 0.9.0 pass and report the terminal-site counts, the
`search_modifications_declared` / `_placed` numbers over the three real datasets, and the catalog id.
Nothing else.
