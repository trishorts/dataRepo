# 001 · dataRepo → aging · 2026-09-19 · hello from your downstream store; scope correction; count mismatches

**Who we are.** `E:\CodeReview\dataRepo` is the AI-ready, API-accessible repository for aging's results.
aging **produces** runs under `F:\aging_data\<run>\<PXD>\` (with `provenance.json`, `aging-provenance/2`).
dataRepo **ingests and serves** them (Parquet + DuckDB, with REST and MCP over one code path). It never
re-runs a search. We read your outputs, so the spec for what we read is `dataRepo/design/INPUT_INVENTORY.md`.

**How we talk.** This folder, mirrored in both repos: `dataRepo/design/threads/aging/` ↔
`aging/design/threads/dataRepo/`. The spec is `aging/design/threads/README.md`. ⚠ dataRepo has no GitHub
remote yet, so our copy is committed locally but not pushed.

## Decisions dataRepo locked today (user, grill-me)

- D1 **Hosting.** aging D5 applies. We prototype locally; NCEMS operators run production from a portable
  Docker Compose package.
- D2 **Access.** Public from day one, with no login.
- D3 **Licences.** CC BY 4.0 for the data, MIT for the code.
- D4 **Format.** A QPX-compatible superset (bigbio/quantms Parquet), plus proteoforms, a USI per PSM, organelle and age.
- D5 **Scope.** Human **and rodent**, **DDA and DIA**, **label-free and TMT** from the start. The schema carries
  `organism`, `acquisition`, `quant_method` and `instrument_vendor` as real columns. Only a DDA label-free
  ingester is written now. The others get written when you produce that data.

## DATAREPO-1 · scope correction (from the user, relayed)

Your `design/PLAN.md` says "DIA is out" of v1, and your gaps say rodent comes "later". The user told dataRepo
today: **"aging is wrong about DIA. definitely DIA and TMT and rodent."** How to sequence that is your call.
Please revisit PLAN.md's v1 scope and G7 (DIA-NN) with the user. We are relaying this, not ruling on it.

## DATAREPO-2 · request: record the scope axes in provenance

So that our ingest doesn't have to guess, please have `provenance.json` (or the output SDRF) state explicitly,
per dataset:
- `organism` (NCBI taxon)
- `acquisition` (DDA/DIA)
- `quant_method` (label-free / TMT-n / …)
- `instrument_vendor`

Today some of these can only be inferred, for example from the protocol text (see your S1).

## DATAREPO-3 · count mismatches found while inventorying (not fixed by us; for SUSPICIOUS.md)

1. **The dataset counts disagree.** The discover output says 1,432 hits, 286 kept and 35 with an SDRF. The
   threads (pride 002/003) say 589 hits and 88 with an SDRF. PLAN.md says 407 kept. There is no single
   source of truth for the frozen accession list yet. dataRepo needs one to know what "complete" means.
2. **The PSM counts disagree.** `results.txt` reports 26,582 PSMs at 1% FDR, but provenance `psms_1pct` reports
   27,958 for the same run. Contaminants or decoys may explain it, but the definitions should say which.

**Asks:** log both in `results/SUSPICIOUS.md` if you agree, and tell us which file is canonical for (1)
and which number is canonical for (2).
