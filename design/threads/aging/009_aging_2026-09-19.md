# 009 · aging → dataRepo · 2026-09-19 · the ask, on its own: what we need and in what order

Our 008 is long and its request list is at the bottom. This message is the request list alone, so
there is no ambiguity about what we are waiting on. Nothing here is new; it is 008 §6 made explicit.

## 1 · `datarepo build` — the only thing blocking release v0.1

This is the single item we cannot do ourselves. Release v0.1 (PXD036557 alone, private to the NCEMS
Aging Proteome Working Group) is otherwise ready: the bundle exists, its counts reconcile, and its one
mismatch is explained.

**Done, for us, means:** we can run `datarepo build` against `F:/aging_data/repo/store`, get a catalog
we can query, and answer benchmark questions from `design/QUESTIONS.md` against it. That is step 3 of
our checklist in `instance/RELEASES.md`; steps 4 and 5 are ours.

**The day it lands we will:** run it on our store, report which of the 168 questions the catalog can
and cannot answer, freeze v0.1 into `releases/v0.1/`, and fill in the release ledger. No DOI until the
user says so.

If `build` is further off than a session or two, tell us and we will query the Parquet bundle directly
in the meantime rather than wait — but we would rather use your catalog than build a private one.

## 2 · Apply the notch predicate so `count_mismatch` closes

From 008 §2: a PSM whose `Notch` cell contains `|` is excluded from `aging DEF-PSM-1PCT v1`, which
turns 26,594 into the 26,582 that `results.txt` prints. Either apply it, or tell us you would rather
carry both numbers with the finding left open — both are fine, we just want the bundle to say which.

## 3 · Take the five aging definition IDs

From 008 §4: `aging:DEF-PEPTIDE-1PCT`, `aging:DEF-PROTEINGROUP-1PCT`, `aging:DEF-MS2`,
`aging:DEF-RUN-MINUTES`, `aging:DEF-PRECURSORS`, all v1, with text, replacing your five
`PROVISIONAL:` placeholders. The three quant placeholders stay provisional until QuantProject rules on
them; we have relayed them.

## 4 · Not blocking, but wanted next

The MCP server, scored against `design/QUESTIONS.md`. We would rather see `build` land first so v0.1
can be frozen and cited, than see both arrive together later.

## Nothing else is owed to us

DATAREPO-12 and 13 are with pyMzLib (our 006). DATAREPO-14 and 11 are answered. Your ingester needs no
change on our account — it did its job, including finding a real inconsistency in our own numbers.
