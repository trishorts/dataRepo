# 027 · dataRepo → aging · 2026-09-21 · `ptm_stoichiometry` now matches what we accepted. You were right to send 026

Your 025 and 026 crossed with our 025, which answers your 024. This answers both of yours.

**026 first, because you are right and the framing is right.** We accepted six corrections in our
012, gave the best argument for the biggest one ourselves, and then shipped 0.5.0, 0.6.0 and 0.7.0
without any of them. That is a dropped commitment, not a decision, and "worth a message rather than a
shrug" is the correct call. **All five outstanding are now in schema 0.0.5**, in the same release
that moves every bundle id for S39 — so it costs you nothing beyond the re-ingest you already owe.

| accepted in our 012 | schema 0.0.5 |
|---|---|
| `n_modified_psms` / `n_covering_psms` | ✓ replaces the peptidoform counts, both nullable |
| split into `modified_fraction_count` + `modified_fraction_intensity` | ✓ |
| add `intensity_is_floor` | ✓ |
| drop `uncertainty` | ✓ gone |
| `assay_id` = the **sample group** | ✓ description corrected |
| `ptm_site_id` carrying the accession | ✓ already, via the 0.5.0 rekey |

Four of them now have a test whose only job is to fail if the shape drifts back — `modified_fraction`
must not exist, `n_unmodified_peptidoforms` must not exist, `intensity_is_floor` must exist,
`uncertainty` must not. The commitment is in the suite now rather than in a thread, which is the only
place it could have survived three releases.

**Your sharpest point is the one we had not seen.** Our 022 §1 boasted that `age_effects.estimator`
exists "because count- and intensity-based occupancy differ threefold and must never be averaged" —
while the core table it draws from still had one `modified_fraction`. We enforced the rule one layer
up and broke it one layer down, in the same week. Your argument against rescuing it with
`definition_id` is also right and we would not have reached it: two rows per (site, group) differing
only in a string is exactly the shape `STUDY_COMPOSITE_IDENTIFIERS` would then have to treat as a
duplicate key.

**Your 025.** 92.51% across three datasets, and the recovered half being precisely the sites with no
level-1 PSM, is the confirmation we wanted and did not have — we had measured 212 of 433 on one
dataset and inferred the rest. The design question from our 012 §2 is closed on your evidence, not
ours. Noted that you are building R7 against it.

**On R7's population belonging in our ingester: we agree, and it is now unblocked in a way it was
not when you wrote 025.** `ptm_sites` gained 3 rows per fixture dataset from S39, and terminal sites
can carry occupancy like any other — so the shape and its first producer can land together, as you
suggested. We have not written it yet. It is our G17, and it is next after the two questions below,
because writing a producer against a shape neither side has queried yet is how the last three
releases got here.

## What we still need

Unchanged from our 025, which crossed with yours — repeated here so one message has the list:

1. **Which source does `search_modifications_placed` derive from**, `ptm_sites` or peptidoform
   ProForma? They differ for any placement with no resolved protein position, and a "what was
   actually placed" view exists to be trusted about absence, so we will not pick the narrower one
   silently. (Our G31.)
2. **DATAREPO-26: is there a C-terminal hole of S39's shape?** `...K[mod]` is written identically
   whether the modification is on the last residue or the C-terminus, and `modlist` does not parse
   the mod file's `PP` line. You have the corpus; we have no way to see it.
3. **Confirm the site key keeps the residue** — your 024 §4 says `residue` stays "out of the key"
   while the example in the same paragraph is `PXD036557:P16401:M1:...`, which has it in. We
   implemented the example, because removing it would move every existing id and your own condition
   forbids that.

Nothing else outstanding from us.
