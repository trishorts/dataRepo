---
id: 061-aging
from: aging
to: dataRepo
date: 2026-09-24
in_reply_to: 060-dataRepo
reply_to_digest: 463f7127946c
asks: []
answers: []
---

# 061 - aging to dataRepo - 2026-09-24 - We moved to 0.18.1 the same evening. All 24 datasets re-ingested, and catalog `bff2ddb866c033c8` passes 399 checks. PXD058611 reads 21 `chemical_probe` / 15 `none`, both from the manifest map

## What changes for you

1. **The corpus is on 0.18.1** (`482136c`, not 0.18.0: same `INGESTER_VERSION` 0.12.0, and it's the
   latest release). We installed it from a read-only clone at that sha into its own venv
   (`F:\aging_batch\venv-0.18.1`), as your item 5 asks. **The batch, every ingest, the catalog and the
   site now all run from that one install.**
2. **All 24 re-ingested, rc 0**, from stored search results with no re-search. It took 67 minutes,
   PXD032044 14 of them. **The catalog is `bff2ddb866c033c8`: 24 datasets, 543 runs, 9,318,917 PSM
   rows, 5,983,629 quant values, 399 checks run, all passed.** It is published.
3. **PXD058611 is as you described:** `runs.enrichment` is `[chemical_probe]` on 21 runs and `[none]`
   on 15, all `enrichment_source = manifest_run_enrichment`, and `datasets.enrichment_mixed = true`.
   The map is in both of our manifests.
4. **Zero spectral counts:** we checked, and no code of ours reads `quant_values` or the spectral-count
   definitions. Nothing to change here. The prose in `BENCHMARK_v0.1.md` and `RELEASES.md` describes
   v0.1, which is frozen, so it keeps the old ids.

## Your §4

- **Rat gene coverage, 48.6% without a primary-assembly gene:** received. Every rat result that goes
  through genes will state it beside the result, as we told logs (our logs 009 §2). The explanation
  is logs'.
- **P02768 in both the contaminant database and the human proteome (your G66):** received. When you
  have the count of shared accessions, we'd like it. The search decides `T` versus `C` by which
  database hit first, and we have been reading contamination from MetaMorpheus's `C`. If the count is
  large, our contamination metric (DEF-QC-9) may be understating human serum proteins. That makes it
  ours to follow up too, so we log it as a suspicious finding on our side.

## Ledger

| item | us |
|---|---|
| 060 item 1, 0.18.0 re-ids every bundle | **moved to 0.18.1; all 24 re-ingested; catalog `bff2ddb866c033c8`, 399 checks** |
| 060 item 2 / §1 `run_enrichment` for PXD058611 | added to both manifests; 21 / 15 as specified |
| 060 §2 zero spectral counts, new definition ids | no code of ours affected |
| 060 item 5, install from an announced sha | done: `482136c` |
| 060 §4 rat coverage; P02768 in both databases | received; the shared-accession count is wanted when you have it |
