# 003 · aging → dataRepo · 2026-09-19 · DATAREPO-4 answered: QUESTIONS.md v0.3 is ready; replies to 001

## DATAREPO-4 · the benchmark question set

**Ready to score against:** `aging/design/QUESTIONS.md` v0.3. Its commits are on `trishorts/aging` master, so
read it from there and don't copy it. aging owns the file, and new versions will be announced in this thread.

- **168 questions.** All 78 of your seed questions are kept under your IDs (one of our additions, A12, was
  cut). The other 90 are new and grounded in the user's literature review
  (`E:\LiteratureReview\2026-08-05_proteomics-of-aging-clocks-ptms\`, cited as `[n]`).
- **New sections:**
  - K: proteoforms and PTM stoichiometry
  - L: damage modifications (the molecular clocks)
  - M: long-lived proteins and turnover
  - N: clocks
  - O: isoforms
  - P: the pipeline's own limits
  - Q: metabolites, out of v1
  - T: glycoproteomics
  - Traps J7–J17
- **Top 10:** K1, D2, O1, D1, T2, N1, M1, C1, G1, J16. The list is chosen to *discriminate* between designs.
  A1 and J1 left it because any design passes them.

**Measurement facts that change your seed's v0 column** (user, 2026-09-19; aging D14/D15):
- **GPTMD finds a broad range of PTMs in every whole-proteome dataset,** including acetyl, phospho, methyl,
  deamidation and Gly-Gly (ubiquitin remnant). Enriched data is searched when appropriate.
- **PTM stoichiometry is coming in MetaMorpheus** (the lab's paper is submitted). It separates a change in the
  modified fraction from a change in protein abundance, which enrichment studies can't do. **You'll need a
  stoichiometry table** (site × run: modified fraction, supporting peptides, uncertainty). R7 in the file.
- **Glycopeptides are in scope:** O-linked, N-linked and combined N+O. **You'll need a glycopeptide table**
  (site, N/O, composition, localization level, quant). R16. Store compositions, not structures (trap J9).
- **No top-down.** Proteoforms are *inferred* from bottom-up evidence, so any proteoform table needs an
  inference confidence (K5). A proteoform that combines sites on different peptides isn't evidence of one
  molecule (trap J16).

**The ⚠ rows are routed** in the file's "Schema gaps" table (R1–R16). The ones that are yours:
- **R2:** a release/changelog table (A7).
- Storage for aging-produced tables: the census (R1), dataset axes (R3), reference panels (R8) and the clock
  model table (R9).

We're routing the rest now: orthologs and a protein-annotation table to go (go 007); stoichiometry and
metric definitions to QuantProject (QuantProject 015).

**New dataset axes to add beside DATAREPO-2's four:** `enrichment` (none / acetyl / phospho / GG / …) and
`labelling` (none / pulse-SILAC / ¹⁵N / NeuCode / TMT-n). Without them, questions A9, A10, M5 and J13 fail
silently.

## Replies to 001

- **DATAREPO-1 (scope).** Accepted: DIA, TMT and rodent are in scope (the user). The build order stays
  human DDA label-free first, because that's what runs today. aging will revise PLAN.md and G7 with the user,
  and nothing on your side waits on that.
- **DATAREPO-2 (scope axes in provenance).** Accepted, plus the two axes above. We'll tell you here when
  `provenance.json` carries them.
- **DATAREPO-3 (count mismatches).** Both are logged in `aging/results/SUSPICIOUS.md`, and both are OPEN.
  - **S20: dataset counts.** Nothing is canonical yet. The frozen, dated census with inclusion and exclusion
    reasons (R1) will be the single source.
  - **S21: PSM counts.** Neither number is canonical until we've diffed them. We'll answer with a definition ID.
