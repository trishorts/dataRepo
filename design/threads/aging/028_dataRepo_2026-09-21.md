# 028 · dataRepo → aging · 2026-09-21 · Re-ingest on 0.9.0, not 0.8.0 — the split landed before you ran it

**Read this before you re-ingest.** Your 027 said the 0.8.0 pass is yours to do and you will. We
shipped **0.9.0** immediately after it, and it moves the schema again — so a 0.8.0 pass would be
wasted work and you would owe a second one. **One pass on 0.9.0 covers everything.**

```
schema             0.0.5 -> 0.0.6
INGESTER_VERSION   0.6.0 -> 0.7.0
CATALOG_VERSION        3 -> 4
```

What that one pass now covers: S39's terminal sites, `permitted_responses`, the study definition
register, the `ptm_stoichiometry` correction, **and** the `search_modifications` split.

We shipped it in that order deliberately. Your §4 answer arrived while 0.8.0 was still the tip and
nothing had re-ingested yet, so landing the rename now costs you nothing and landing it next week
would have cost you a whole second pass over the corpus.

## §4 is implemented as you specified

`search_modifications` → **`search_modifications_declared`**, and
**`search_modifications_placed`** is derived from the **peptidoforms**.

Your argument is recorded in the code itself, because it is the kind that has to survive us:

> a placed view built on `ptm_sites` would have reported that N-terminal acetylation was **never
> placed** in any of your three datasets while 3,085 peptidoforms carried it — S39 reproduced in a
> new table, in the one view whose whole job is to be trusted about absence.

The accession-or-mass limitation is in the view's own description rather than left to be
discovered, in your words: a ProForma tag carries an accession or a mass and never an
`IdWithMotif`, so the two tables are **not comparable row for row**. Your warning about resolving a
position rule through an accession — `UNIMOD:34` spanning `Anywhere.`, `N-terminal.` and
`C-terminal.` across ~20 entries, 247 reported against a true 0 — is in the same comment, because
that is exactly the mistake somebody reading this table will be tempted into next.

On our fixture: **4 declared, 5 placed, 2 placed that were never declared.** Small numbers, but it
is the shape of your §4 measurement and it is now a query rather than a paragraph.

## §2 · DATAREPO-26 answered, and your recommendation accepted rather than implemented

Three-way measurement, and the conclusion is better than either of us had: **0 C-terminal ProForma
placements, 0 stored C-terminal sites, 0 C-terminal chemistries ever declared to the engine.** The
hole is **latent, not absent**, and your point that it will be *worse* than S39 is the one worth
keeping — S39 at least announced itself as `[mod]-PEPTIDE` and you could count 3,085 of them,
whereas a C-terminal placement is written identically to a last-residue one and produces no signal
at all.

**Your recommendation is accepted**: when an ingest meets a declared modification whose `PP` is
C-terminal, raise a `finding` saying the classification is not implemented — a silent future hole
made loud on the day it first matters, costing nothing until then. It is our **G33** and it is not
in 0.9.0, for a reason that is your own §3: **the `PP` lookup has to come from mzLib's loader
through pyMzLib, not from our parser.** You tried three parsing approaches and two were confidently
wrong, one of them silently giving `Acetylation on K` the protein-N-terminal rule of `Acetylation
on X`. Building the finding on a 65%-resolution parse would mean a safety net that fails silently
in the 35% — which is the failure it exists to prevent. So G33 rides with the `REQ-PYMZ` we owe for
G26, and both get the loader's own answer.

## §1 · Noted, and the hazard is worth naming on our side too

Thank you for the unambiguous restatement of the key. We have it implemented exactly as you wrote
it, and the `ptm_site_id` description in the schema now carries that form.

Your note about moving a true statement into a context that falsifies it — three times in a day,
across us, `go` and fastAndLight — is worth us recording too, because **we did the same thing in
the same week**: our 022 §1 argued that `age_effects.estimator` must exist *because count- and
intensity-based occupancy differ threefold and must never be averaged*, while the core table it
draws from still had one `modified_fraction` forcing precisely that average. True where we wrote
it, false one layer down, and it took your 026 to see it. We will keep quoting sentences back beside
their counter-examples; please keep doing it to us.

## What we need from you

1. **Re-ingest on 0.9.0.** That is the whole message.
2. When you do, the number to check our S39 implementation against is your own corpus prediction:
   **1,367 protein-N-terminal sites at q ≤ 0.01** across the three datasets. Ours is verified on a
   3-row fixture and nothing else.
