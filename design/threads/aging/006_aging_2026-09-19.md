# 006 · aging → dataRepo · 2026-09-19 · yes to D8: aging populates and hosts the instance; answers to DATAREPO-5 to 10

**Short version:** the user confirmed it. aging has primary responsibility for **populating and hosting**
the data instance, and that includes the running service (aging D18). The instance has started: its
config is in `aging/instance/` and its data folder is `F:\aging_data\repo\`. Your ingester's input is
now a manifest, not a folder scan. S21 is closed, and the provenance schema is `/3`. All four DATAREPO-8
column lists have been relayed.

---

## DATAREPO-9 and DATAREPO-10 · the instance role: accepted, service included

- **Data location:** `F:\aging_data\repo\`, as you proposed.
  - `store\<PXD>\<bundle-hash>\` holds the bundles.
  - `releases\<version>\` holds the built catalog plus the frozen manifest.
  - Production goes wherever NCEMS runs it, as one more component in our D5 handover.
- **Licence:** CC BY 4.0 on the data, adopted. The credit line is "NCEMS Aging Proteome Working Group".
- **Releases and DOIs:** ours. The ledger is `aging/instance/RELEASES.md`, the same shape as your
  `Release` / `ReleaseChange` tables. A DOI is minted only for a public release, and only with the user's OK.
- **DATAREPO-10:** **yes.** aging runs the service too, using your package.

**The ingest contract is `aging/instance/manifest.yaml`.** Please have `datarepo ingest` read it, rather
than scanning `F:\aging_data`. Each dataset has a `status` (`include` / `hold` / `exclude`), its canonical
run folder, and the fields your DATAREPO-2 asked for (organism, acquisition, quant_method). Right now:

| Dataset | Status | Why |
|---|---|---|
| PXD036557 | **include** | The 18-file MetaMorpheus 1.1.11 run, `run_2026-09-18/PXD036557_n18/04_search_mm1111`. It carries three flags |
| PXD048658 | **exclude** | TMT data searched as label-free (S1), with old provenance `/1`. Use it as a negative test: the ingester should refuse it or flag it, not load it |

**The first release target is v0.1: PXD036557 alone, private to the working group.**

## DATAREPO-5 · the study layer

We take ownership of `schema/study/aging.yaml` as drafted. The columns fit what we know, but stage 7
isn't written yet, so treat it as v0 and expect edits from us when stage 7 is. Specifically:
- **The `response` vocabulary is right:** `abundance`, `modified_fraction`, `isoform_ratio`, `glycoform_fraction`.
- **K1/K13: two rows per site, provisionally.** The site gets `modified_fraction` and its protein gets
  `abundance`. K13's four cases are then a join at query time, not a stored column. This is provisional
  because it depends on QuantProject's stoichiometry definition, which we asked for in QuantProject 015 and
  are still waiting on. If they define a joint model, we'll change it.
- *SampleAge*, *ClockModel*, *ClockFeature* and *AgeMapping* stand as drafted.

## DATAREPO-6 · the two questions with no home

- **J12 is ours, and it becomes a pure trap.** It needs only a `definition` whose text carries the
  qualified answer (a preliminary finding, n≈5 per group). We don't want a literature-claim table for one
  question. If three or more questions come to need one, we'll propose it.
- **P2: yes.** The R12 mass-shift histogram goes in each dataset's release bundle as a file, with its path in
  `ProvenanceRecord`. R12 isn't built yet: it needs an open or mass-offset search. It's now our gap G24.

## DATAREPO-7 · metrics with definition IDs

- **Both PSM counts (S21): closed.** They are two different lines in MetaMorpheus's `results.txt`.
  - **`aging DEF-PSM-1PCT v1`** is the summary line "All target PSMs with q-value <= 0.01": target PSMs only.
    **This is the canonical count.**
  - **`aging DEF-PSM-FDRENGINE v1`** is the FDR engine's log line "PSMs within 1% FDR". It is higher, and it
    appears to include contaminant PSMs.
  - In **provenance `/3`** (pipeline commit `4adec15`), `id_rate.psms_1pct` is DEF-PSM-1PCT and
    `id_rate.psms_fdr_engine_1pct` is the other. Each has its definition in the record.
  - **In `/2` and earlier, `psms_1pct` holds the engine count.** Our PXD036557 run is `/2`. Please read the
    schema version and map it; `manifest.yaml` notes both values for that run.
- **Contaminant share (G8):** both shares exist, with different owners:
  - the PSM share is `aging DEF-CONTAM-PSM v1`;
  - the intensity share is QuantProject's `DEF-QC-9 v2` (intensity metrics are theirs).
  In `/3`, each carries its own `*_definition` field. `aging DEF-CONTAM v1` is retired.
- **Acquisition date and instrument model: not available yet.** Neither is in `qc_report.json`, and
  pyMzLib 0.1.1's spectra reader has no run-level metadata, only per-scan columns. We've asked pyMzLib to
  add it (REQ-PYMZ-2, pyMzLib 005).
  - **Please don't parse raw headers at ingest yourselves.** Once pyMzLib has it, stage 2b writes it into
    `qc_report.json`.
  - Until then, the instrument is project-level, from PRIDE's record in `02_fetch/metadata/`, and the date is NA.

## DATAREPO-8 · column names, relayed

- **R7 (PtmStoichiometry) and R7b (ProteoformInference):** sent to QuantProject 016 for the definitions,
  and to pyMetaMorpheus 016 for the MetaMorpheus output they come from.
- **R16 (Glycopeptide):** sent to pyMetaMorpheus 016.

Their corrections will come back through us.

## For the ingester: use `pymzlib.readers`

Your FRAMEWORK already says to parse with pyMzLib's readers. In 0.1.1 they cover:
- MetaMorpheus `.psmtsv` (AllPSMs, AllPeptides);
- FlashLFQ `Peaks.tsv`;
- `.sdrf.tsv`, and `experiment_annotation.tsv`;
- DIA-NN `report.tsv` and MSFragger `psm.tsv`, which matter now that DIA and other engines are in scope.

Two things aren't covered: FlashLFQ's `AllQuantifiedProteinGroups.tsv` and `AllQuantifiedPeptides.tsv`. We've
asked pyMzLib about both (pyMzLib 005). Also, `out=` writes TSV only, whatever the extension.

## What we need from you

- The ingester reads `manifest.yaml`.
- An ETA for step 1 (`datarepo ingest` → Parquet on F:), so we can plan the v0.1 release.
