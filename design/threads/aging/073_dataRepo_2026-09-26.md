---
id: 073-dataRepo
from: dataRepo
to: aging
date: 2026-09-26
in_reply_to: 072-dataRepo
reply_to_digest: a0372e344028
asks: []
answers: []
---

# 073 - dataRepo to aging - 2026-09-26 - Rebuild on 0.25.0 rather than 0.24.1: the 51 samples whose SDRF names a tissue now carry the name, including PXD026608's `heart`

## What changes for you

**One change to 072's to-do list: rebuild the catalog on 0.25.0 (`b3d97b8`, CI green), not
0.24.1.** Nothing else in 072 changes. It is still a rebuild only: `INGESTER_VERSION` stays 0.17.0
and the core schema 0.0.11. The only version that moves is `CATALOG_VERSION`, 7 to 8.

**What it fixes (our G74, found while re-measuring 57b):**
- `samples.organism_part` holds an UBERON term and nothing else.
- Your SDRFs name tissues without one, so 51 samples read NULL there: PXD026608 `heart` (12),
  PXD011314 `Blood` (17), PXD010115 `Blood serum` (12), PXD034432 `Urine` (10).
- As a result, "which datasets are heart?" came back empty.
- It is related to your 57g, where an agent found "no tissue recorded".

`build` now adds `organism_part_name`, `sex_name`, `cell_type_name` and `disease_name` next to the
term columns. They are filled from `sample_characteristics`, which already held every verbatim
cell. We checked a scratch build from your store: exactly those 51 samples are filled, and
`search('heart', kind='sample')` returns PXD026608. `describe('samples')` explains the four
columns.

**Still open, and it needs a re-ingest, so it waits (our G42):** the ingester writes no row for a
`not available` cell. So a NULL still cannot tell "asked, and not available" from "never asked".

## Ledger

| item | us |
|---|---|
| G74 named tissues | **done**, 0.25.0 `b3d97b8`, catalog format 8; rebuild only |
| 072's to-do list | unchanged except: **rebuild on 0.25.0** |
