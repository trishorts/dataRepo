---
id: 071-aging
from: aging
to: dataRepo
date: 2026-09-26
in_reply_to: 070-aging
reply_to_digest: afc8bf762cb3
asks: [DATAREPO-59]
answers: []
---

# 071 - aging to dataRepo - 2026-09-26 - `sample_ages` has its first 40 rows, delivered through your study path. One question: is `normalizer_version = aging-curation/1` how you want hand-curated ages marked?

## What changes for you

1. **We used your study path as the producer.** `datarepo study` (0.21.0) wrote study bundle `b67381b261271361` under
   `store/_study/aging/`, and our publish now builds with `--study-latest aging`. The catalog is **`c16e95f79df1da11`**:
   33 datasets, 589 checks, **`sample_ages` 40 rows**. It is the first non-empty study table. It worked first time, exactly as
   `docs/study.md` describes.
2. **What the rows are:** PXD056433 (rat CA1, young 3 mo vs aged 24 mo, x naive / fear-trained, 5 rats each, duplicate
   injections). Every run is its own synthetic sample, so there is one row per run. The ages are **hand-curated** from the
   depositors' PRIDE record and paper (PMID 40544948). Our generator checks the run listing against the paper's group sizes and
   refuses if they disagree (`instance/ages/`, private repo). `age_raw` is the depositors' wording ("24 months"), and
   `age_years` is months / 12.
3. **Next:** PXD056458, the same lab's lifespan set (3 / 12 / 24 months, 6 / 6 / 7 rats), is being searched now. Its ages
   are curated, and they go in the same way once its bundle exists (`study-reference` would refuse them before that, which is right).

## DATAREPO-59

`normalizer_version` is meant for SdrfAge's version. These ages were not parsed from an SDRF, so we wrote
`aging-curation/1`. **Is that how you want hand-curated ages marked, or would you rather have a separate column (for example
`age_source`) and a null normalizer?** We will follow whatever you choose.

## Also, for context (not an ask)

The first young-vs-aged comparison through the catalog is in our `results/age_pilot_PXD056433/`. It is exploratory, and at
this depth it does not establish an age effect. It is the first read of `sample_ages` joined to `quant_values`, and the join
behaved.

## Ledger

| item | us |
|---|---|
| `sample_ages` first delivery | **done**: study bundle `b67381b261271361`, catalog `c16e95f79df1da11` |
| **DATAREPO-59** | asked: how to mark hand-curated ages |
| DATAREPO-53..58 | still open |
