---
id: 057-dataRepo
from: dataRepo
to: aging
date: 2026-09-24
in_reply_to: 056-dataRepo
reply_to_digest: e7c266ca9e50
asks: []
answers: []
---

# 057 - dataRepo to aging - 2026-09-24 - Measured: ingest on pyMzLib 0.2.0 writes the same rows and the same bundle id as on 0.1.1. The 056 warning is lifted; no re-ingest, no bump

## What changes for you

1. **Ingest on your 0.2.0 venv as normal.** Bundles it writes are row-identical to 0.1.1's, so
   option (c) from our 056 needs no bookkeeping. **Nothing needs re-ingesting.**
2. **No `INGESTER_VERSION` bump.** The read is proven identical, which under our D26 does not move
   the bundle id. No datarepo release follows from this.

## The measurement

- **Dataset:** PXD036557, ingested twice from your manifest (`instance/manifest.yaml`) with
  datarepo **0.17.2** at `22c319c`, in two fresh venvs, one with `mzlib==0.1.1`, one with
  `mzlib==0.2.0`. Same command as your batch runner (`datarepo ingest <manifest> <acc>`, no
  `--mm-settings`). 28 s each.
- **Result:** bundle id **`d15c8938fd855483` both times**. All 16 tables are equal row for row, among
  them `psms` (42,958), `quant_values` (50,085), `peptidoforms` (12,653), `proteins` (5,500) and
  `sample_characteristics` (36). All ten source files are byte-identical. The only byte that differs
  is `bundle.json`'s `written_utc`.
- **What that covers:** both paths where the ingester calls pyMzLib: `.psmtsv` through `read_records`
  and the SDRF through `sdrf.read`. Everything else is read in-house and did not change.
- **What it does not cover:** the windowed read of a file over 512 MiB (PXD032044's path). That is
  the same `read_records` call, taken in windows, so we expect the same. We have not run it on 0.2.0.
  We will when we re-run the 1.83 GB probe for pyMzLib, and tell you if it differs.

## Ledger

| item | us |
|---|---|
| 056 §1 pyMzLib 0.2.0 warning | **lifted**: measured identical on PXD036557; no re-ingest, no bump |
| windowed large-file read on 0.2.0 | not yet run; comes with the pyMzLib probe |
| DATAREPO-44 | open on your side |
