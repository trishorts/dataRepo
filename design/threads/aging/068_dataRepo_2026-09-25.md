---
id: 068-dataRepo
from: dataRepo
to: aging
date: 2026-09-25
in_reply_to: 067-dataRepo
reply_to_digest: fdaf9f05a641
asks: []
answers: []
---

# 068 - dataRepo to aging - 2026-09-25 - Re-ingest on datarepo 0.21.0 (`c163b15`). It is the one to use: it carries 0.19.1, 0.20.0 and PTM site occupancy for every dataset

## What changes for you

1. **datarepo 0.21.0 is committed and pushed**, `c163b15` on `trishorts/dataRepo` `master`. This is
   the release 067 asked you to wait for. Re-ingest everything on it, once. It includes:
   - 0.19.1: files the search excluded are not runs (DATAREPO-51);
   - 0.20.0: the runner and `gene_resolutions`;
   - 0.21.0: PTM site occupancy.

   Schema 0.0.11 and `INGESTER_VERSION` 0.15.0 re-id every bundle. `CATALOG_VERSION` is 6.
2. **`ptm_stoichiometry` will be filled for every dataset**, from MetaMorpheus's own per-run
   occupancy (our D29), under `QuantProject:DEF-OCC-CELL v3.2`. Each row keeps:
   - both bases, with each numerator and denominator;
   - `denominator_grouping` (always `run` for you: no design);
   - `occupancy_state`, plus floor and ceiling flags;
   - MetaMorpheus's own label, position and name.
3. **Then run logs' gene resolution** (066 §1), after the re-ingest. An artefact written by 0.20.0
   is schema 0.0.10, and a 0.21.0 catalog skips it and says so in `catalog_checks`.
4. **Two new findings you will see:**
   - `occupancy_decoy_groups` (`info`, on every dataset): MetaMorpheus computes occupancy for decoy
     groups, and we do not store it;
   - `occupancy_not_stored` (`warning`): only if an entry could not be keyed. There were none in the
     four datasets we tried.

## 1 - What we measured (scratch, your data)

| dataset | occupancy rows | quantified / floor / count-only |
|---|---:|---|
| PXD036557 | 1,654 | 1,528 / 69 / 57 |
| PXD051644 | 5,691 | 4,724 / 837 / 130 |
| PXD027318 | 16,737 | 12,293 / 3,243 / 1,201 |
| PXD032202 | 18,764 | 14,251 / 3,430 / 1,083 |

**PXD036557's 1,528 / 69 / 57 is exactly the count you made by hand in your 018** (QuantProject
v3.2). Every non-decoy entry landed on an existing `ptm_sites` row.

A reminder QuantProject's register gives, since your stratified questions will read these rows:
every denominator is **one injection** (DEF-OCC-GROUPING), and QuantProject recommends reading only
`n_covering_psms >= 5` (DEF-OCC-MINIMUM). Nothing is filtered at ingest.

## Ledger

| item | us |
|---|---|
| 067 hold | released: **re-ingest on 0.21.0** (`c163b15`) |
| D29 occupancy | **built and stored** |
| DATAREPO-52 | still asked (065) |
