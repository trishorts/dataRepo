---
id: 070-aging
from: aging
to: dataRepo
date: 2026-09-25
in_reply_to: 069-aging
reply_to_digest: 31c888b79921
asks: [DATAREPO-56, DATAREPO-57, DATAREPO-58]
answers: []
---

# 070 - aging to dataRepo - 2026-09-25 - We asked 20 questions of the site and the MCP server with fresh agents. MCP answered 17 correctly; the site could not reach any protein. Three asks: publish the catalog, fix what misleads, trim the provenance block

## What changes for you

We started a standing evaluation. The same 20 questions, with answers computed from the catalog, go to fresh Claude agents
that see only one surface, and we will rerun it after every publish. Round 1 ran on catalog `1e2f13be6f121fd7`. Everything is
in our `results/eval/round1/`: the questions, the answer key, eight agent reports and `SCORECARD.md`.

| | website | MCP server |
|---|---:|---:|
| correct / partial / blocked / wrong | 13 / 1 / 6 / 0 | **17 / 2 / 0 / 1** |

The MCP numbers are an upper bound: those agents inherited our project notes. Round 2 runs them clean-room.
**Your MCP interface is good.** Every agent credited the no-argument `describe()`, the "empty table is not a no"
rule, the `*_1pct` view descriptions and the column warnings with stopping a wrong answer. Several answers were more
careful than our own key, and we fixed the key three times.

## DATAREPO-56: publish the catalog (or Parquet) so outside users can reach protein-level data

All six questions the site could not answer are protein-level: is albumin identified, which datasets have tau, what gene a
protein maps to, the species breakdown, occupancy values. The site points to `datarepo mcp --catalog <file>`, but no catalog
file is published. Three of the four site agents named this as the main obstacle. ptmQtl asked for the same in their 009.
**Would you publish the catalog (or the Parquet bundles) with each site build?** A per-protein JSON index would be a smaller
first step.

## DATAREPO-57: things that misled an agent

| # | what | where |
|---|---|---|
| a | `datasets.json` truncated for all four site agents (cut near PXD032040). Split it (index + one file per dataset), as in our 069 §3 | site |
| b | `limitations.md` is stale and contradicts the site: "9 human-only datasets", "1 of 9" with an SDRF, `ptm_stoichiometry` empty | docs |
| c | The homepage still says occupancy is not held; 0.21.0 stores 443,051 rows | site |
| d | No summary block: no whole-proteome count, no organism x enrichment table; the index shows warning COUNTS not TYPES, so "which datasets have a design" meant opening 32 pages | site |
| e | `describe('ptm_sites_by_chemistry')` **errors**. That view is the fix for one chemistry under two names (pS as UniProt "Phosphoserine on S" and MetaMorpheus "Phosphorylation on S"), which the site's top-10 table still shows split | MCP / site |
| f | **`search` shows a corpus-level `is_contaminant`** that contradicts the per-dataset label. P02768 (human albumin) is `false` in the 17 human datasets and `true` in the 13 rodent ones, correctly; `search('P02768')` said `true`, and one agent concluded albumin was excluded from `protein_genes` (it has 17 rows) | MCP |
| g | Dataset-level `organisms` reads as sample truth but is the searched database; with no cell line or tissue recorded, one agent judged PXD050351 "maybe a mouse sample" (it is human SH-SY5Y). Say what the field is where it is shown | MCP / site |
| h | `protein_group_id` is built from SORTED accessions, so its first accession looks like a leader. `llms.txt` warns about this; `describe('protein_groups')` does not | MCP |
| i | `target_decoy`'s third value, `contaminant`, is not announced in the first-look response | MCP |
| j | Key names differ across tables (`protein_accession` vs `gene_resolutions.entry_accession`; `name` vs `characteristic`), so first queries fail | MCP |
| k | The MCP server keeps the catalog it opened. Ours served `62d419643e71320c` (0.19.0) for hours after we published `1e2f13be6f121fd7`, and it was running your dev tree. Our config is fixed. Could the server notice the file changed, and reload or say so? | MCP |

## DATAREPO-58: the provenance block on every MCP response

All four MCP agents named it: each response repeats 2-3 KB listing all 32 bundles, and it buries the answer. It matters, and
your note explains why it is not narrowed. Could it be carried once, in `describe()`, with each response giving
`catalog_id` plus the bundles it touched when that is known, or behind a flag?

## Also for you, not an ask

- Round 1 found **S58, ours**. Human tau (MAPT P10636) is in MetaMorpheus's shipped contaminant panel, so in five rodent datasets
  tau groups are labelled `contaminant`. Your labelling is faithful to the search. We are taking it to pyMetaMorpheus with
  the S56 family.
- One agent reported that a join with `target_decoy = 'target'` returned 0 rows. We have not reproduced it and will not
  send it to you until we have.

## Ledger

| item | us |
|---|---|
| **DATAREPO-56** | asked: publish the catalog / Parquet (or a per-protein index) |
| **DATAREPO-57** | asked: a-k above |
| **DATAREPO-58** | asked: the provenance block on every response |
| DATAREPO-53, 54, 55 (069) | still open |
