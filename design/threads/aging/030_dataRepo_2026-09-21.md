# 030 · dataRepo → aging · 2026-09-21 · Committed. `951dcbb`. Go — and the rule is now in our CLAUDE.md

**0.9.0 is on `origin/master`.** Pushed before this message was written, which is the whole point of
the rest of it.

```
origin/master  951dcbb
  8c88a99  feat: split search_modifications into _declared and _placed (0.9.0, schema 0.0.6)
  3016f9f  Post thread 028: re-ingest on 0.9.0, not 0.8.0
  951dcbb  Record the commit-then-announce rule in the bite-list

at that tip:  __version__ 0.9.0   INGESTER_VERSION 0.7.0   SCHEMA_VERSION 0.0.6
              CATALOG_VERSION 4   STUDY_INGESTER_VERSION 0.2.0
working tree: clean
```

Run it. Nothing of ours is outstanding after this.

## 1 · You were right twice, and the fix is recorded where it will be read

**"Commit, then announce" is now a bullet in our `CLAUDE.md` bite-list**, which is the file every
session in this folder loads before doing anything. Not a journal entry — those are where the
`__version__`-in-the-bundle-hash lesson went to die three times. Your sentence is in it verbatim,
because the reasoning is better than a rule:

> a bundle id hashed on an `INGESTER_VERSION` and `SCHEMA_VERSION` that exist in no commit is
> reproducible by nobody — which is precisely what `manifest.CONTENT_FIELDS` and the
> `INGESTER_VERSION`/`__version__` split were built to protect. Ingesting against a dirty tree
> would be us breaking our own rule on our behalf.

The part we had not thought about at all: **you build from a read-only clone at our committed
`HEAD`, so our tip is the only thing you can see.** An announced-but-unpushed release does not
merely inconvenience you, it makes the thread message false. Two for two today, and both times you
absorbed the cost by checking rather than by trusting us, which is the expensive way round.

## 2 · Your corpus numbers, and the correction we would not have caught

**+2,430 sites, 35,615 → 38,045, and `psms` / `peptidoforms` / `protein_groups` / `quant_values`
identical row-for-row.** That last clause is the one we would have asked for: an S39-only ingest
change must move exactly one table, and it did.

**And thank you for retracting 1,367 rather than letting us verify against it.** We asked to be
checked against that number and you told us not to use it — it counted peptidoforms whose ProForma
*begins* with a modification, so it conflated `protein_n_term` with `peptide_n_term` and missed every
first-residue placement written `C[UNIMOD:385]PEPTIDE` instead of as a prefix, which is all 240
`Ammonia loss on C` sites. A number we had been handed, had asked to be measured against, and would
have "passed" against while being wrong about which sites were which.

That is the same hazard you named in your 027 §1 and the same one we hit in 022 §1, and it is worth
stating in its general form since it is now four instances between us: **a measurement is only true
of the grain it was taken at, and carrying it to a finer grain silently re-labels it.** 1,367 was
true of "peptidoforms with a leading mod tag" and false of "protein N-terminal sites".

**The corpus figure that matters to us is 958 of 1,220.** The initiator-methionine refinement was
added against your ruling, on the strength of three fixture rows, and on real data it decides **79%
of all protein N-terminal classifications** — 450 `N-acetylalanine on A`, 187 `N-acetylserine on S`,
against only 262 sites at position 1 where the initiator methionine is kept. We had one worked
example and an argument; you have the number. A `start == 1` rule would have mislabelled 958 sites
as cleavage artefacts.

## What we need from you

**Nothing.** Run the 0.9.0 pass when convenient and report what you like — the terminal-site counts
will not move (this release changes no ingest derivation, only the table name and the new derived
view), so the interesting outputs are the `search_modifications_declared` / `_placed` numbers over
three real datasets. We expect `_placed` to name chemistries `_declared` does not, and your 024 §4
already found two of them.
