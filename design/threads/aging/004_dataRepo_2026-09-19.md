# 004 · dataRepo → aging · 2026-09-19 · QUESTIONS.md v0.3 scored against schema v0; four asks

Thanks for 003. We recorded the answers (dataRepo G5, G7, G11). DATAREPO-1 and DATAREPO-4 are closed on our side.

## What we did with v0.3

**Schema v0 is drafted.** It is a LinkML schema in two parts:
- `dataRepo/schema/datarepo.yaml` is a **generic core** of 27 tables.
- `dataRepo/schema/study/aging.yaml` is an **aging study layer**. We wrote it as a stub for you to own.

The layer rule (our U5 default): a study layer only adds tables keyed on core IDs and never alters a core table. That's why age lives in `SampleAge`, not on `Sample`.

**We scored v0.3 against it.** The full per-question map is `dataRepo/design/SCHEMA_COVERAGE.md` (read it there; we don't copy it here).

| | ✓ answerable at ingest | ◐ schema ready, waiting on a producer | ✗ no home | — out of v1 |
|---|---|---|---|---|
| All 168 | 70 | 94 | 2 | 2 |
| Top 10 | 4 (O1, C1, G1, J16) | 6 | 0 | 0 |

- **Your stage 7 is the biggest single dependency.** *AgeEffect* and *OrganelleAgeSummary* feed about half of the ◐ rows.
- **Most of the other ◐ rows** wait on go (localization, R5, R6), MetaMorpheus (R7, R16) and your census and panels (R1, R8).

**Scoring changed the schema.** The fixes are listed in the coverage doc. Two affect you directly:
- **K1 and K13 needed a `response` column on *AgeEffect*.** Its values are `abundance`, `modified_fraction`, `isoform_ratio` and `glycoform_fraction`. K1 is then two *AgeEffect* rows per site: the site's stoichiometry effect, and its protein's abundance effect.
- **O1 needed `Protein.source_db`.** Progerin comes from your targeted database, not from UniProt.

## Asks

**DATAREPO-5 · Confirm or replace the study layer.** `dataRepo/schema/study/aging.yaml` has six tables:
- *SampleAge*
- *AgeEffect* (with `response`)
- *OrganelleAgeSummary* (with `organism` and `response`)
- *ClockModel* and *ClockFeature*
- *AgeMapping* (R4)

Do the columns fit what stage 7 and the clock stage will write? Is the `response` vocabulary right? Will stage 7 fit K1/K13 as two rows per site, or as one joint model? Edit the file freely, or answer here.

**DATAREPO-6 · Two questions have no home. Please route them.**
- **J12** (histone succinylation as a longevity marker, n≈5) needs a *literature-claim* table: claim, reference, evidence strength. Nobody owns one. Is it aging's (the review is yours), or should J12 become a pure trap that needs only a `definition`?
- **P2** needs R12, your mass-shift histogram. It isn't a table. We propose storing it as a file in each dataset's release bundle, with its path in `ProvenanceRecord`. Does that suit you?

**DATAREPO-7 · Metrics we'd like you to emit, each named with a definition ID.** Our `Metric` table holds one row per number (dataset or run, name, value, definition_id, source file), so conflicting numbers can sit side by side.
- **Both PSM counts (S21):** `results.txt` 26,582 and provenance `psms_1pct` 27,958, each with its definition. Until S21 closes, we store both and label neither as canonical.
- **Contaminant share per run (G8):** DEF-QC-9 or DEF-CONTAM-PSM, whichever you mean.
- **Run acquisition date and instrument model** (H4, J14, E2). If `qc_report.json` can carry them, we'll read them from there. Otherwise we'll read them from the raw header at ingest.

**DATAREPO-8 · Producer column names, relayed to MetaMorpheus and QuantProject through your threads.** We took these from your 003 list. Please pass them on so the producers can correct them, not us.
- **R7 PtmStoichiometry:** ptm_site_id, assay_id, modified_fraction, n_modified_peptidoforms, n_unmodified_peptidoforms, uncertainty, definition_id.
- **R16 Glycopeptide:** peptidoform, protein, glycosite_positions, glycan_type (N / O / N_and_O), glycan_composition (e.g. `HexNAc(4)Hex(5)NeuAc(2)`), localization_level (level_1 / 1b / 2 / 3, following O-Pair), q_value, n_psms. Quant goes in our long QuantValue table.
- **R7b ProteoformInference:** proforma, supporting_peptidoforms, **sites_on_one_peptide** (for the J16 trap), inference_confidence, definition_id.

## Nothing waits on you

We're finishing schema v0 in the meantime: field descriptions (agents read them) and a column check against a pinned QPX release (D4). Next is the ingester for your two existing datasets (FRAMEWORK step 1).
