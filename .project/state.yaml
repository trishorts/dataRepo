name: dataRepo
created: 2026-09-19
phase: INCEPTION

goal: >
  Design and build an AI-ready, API-accessible data repository that organizes the search + quant
  results (PSMs, peptides, proteoforms/PTMs, protein groups, FlashLFQ quant, provenance, SDRF design,
  GO/organelle annotation) from dozens-to-hundreds of PRIDE datasets reanalyzed by the aging pipeline,
  so humans and especially AI agents can query aging-related proteomic changes across organelles.

# SIBLING of aging: aging PRODUCES results (pipeline); dataRepo STORES + SERVES them.
#   aging -> run outputs on F:\aging_data\<run>\<PXD>\ with provenance.json (aging-provenance/2)
#   dataRepo ingests those; it never re-runs searches.
depends_on: [aging]

decisions: []

gaps:
  - "G1 Framework: PROPOSED 2026-09-19 in design/FRAMEWORK.md v0 (Parquet files, DuckDB catalog, FastAPI + MCP over one code path, LinkML schema, QPX superset, USI per PSM). The user has not reviewed or locked it. Next: /grill-me on it, then record the locked choices as decisions."
  - "G5 Step 0 question set: write 50-100 real aging/organelle questions the repo must answer (e.g. 'which mitochondrial proteins decline with age in skeletal muscle in >=3 datasets?'). This comes BEFORE schema or tools; it is the evaluation benchmark (Open Targets Karenina pattern)."
  - "G6 QPX compatibility + v0 scope (human, DDA, label-free, same as aging v1): recommended in FRAMEWORK section 7; confirm in the grill-me."
  - "G7 Count mismatches found in aging (not fixed here; tell aging): discover says 1,432 hits/286 kept/35 SDRF, the threads say 589/88, PLAN.md says 407 kept; results.txt says 26,582 PSMs at 1% FDR vs provenance psms_1pct 27,958. Not yet posted to aging's SUSPICIOUS.md or threads."
  - "G8 GitHub remote: none yet. Offered trishorts/dataRepo (private); the user has not answered. Create only on explicit OK."
  - "G2 Hosting: where does the server live (lab Linux box, UW CHTC/DoIT, NCEMS cyberinfrastructure, cloud)? aging D5 says production goes to server operators."
  - "G3 Access policy: public from day one, or private to the NCEMS working group until publication?"
  - "G4 .gitignore template ignores bin/ (same as aging G21); add !/<dir>/bin/ exceptions if code lands in a bin/ folder."

skip_log: []

repos: []

github:
  remote: ""
  private: true

dir_map: {}

normalizations: []
